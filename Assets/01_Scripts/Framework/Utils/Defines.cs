public enum WorkType
{
    Payment,
    Clean,
    Hall,
    Kitchen,
}

public enum WorkStatus
{
    Waiting,
    Assigned,
}

public enum InteractStatus
{
    Pending,
    Progressing,
    Success,
}

public enum LanguageType
{
    Korean,
}